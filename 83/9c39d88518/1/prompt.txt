In MovieSwift/MovieSwift/views/components/myLists/MyLists.swift, the macOS `customListsRows` ForEach now adds `.accessibilityRepresentation { Button(list.name) { selectedCustomList = ... } .accessibilityIdentifier("myLists.customList.\(list.id)") }` so its rows are reachable by VoiceOver and queryable in UI tests. The sibling `moviesRows` ForEach (used for the Wishlist and Seenlist sections, ~line 384) uses the same `.onTapGesture`-driven row pattern with only `.accessibilityIdentifier("myLists.movie.\(id)")` and no `.accessibilityRepresentation`. Those rows wrap `MovieRow` (a richer component than the plain-HStack CustomListRow, so they MAY already surface — verify first).

Task: (1) Write a failing macOS XCUITest in MovieSwift/MovieSwiftUITests/MovieSwiftMacUITests.swift that launches My Lists (the smoke fixture seeds wishlist [0] and seenlist [0]) and asserts `app.identifiedElement("myLists.movie.0")` exists on the Wishlist segment. (2) If it fails, apply the same `.accessibilityRepresentation { Button(...) { selectedMovie = MovieNav(id: id) } .accessibilityIdentifier("myLists.movie.\(id)") }` pattern to `moviesRows` for parity. (3) Follow the project TDD + delivery workflow in CLAUDE.md (branch, test-first, lint via ./scripts/lint.sh, code-reviewer subagent, PR via gh, green required CI before merge). Note: on macOS this view now uses an eager VStack (not LazyVStack) precisely so rows acquire accessibility identity.