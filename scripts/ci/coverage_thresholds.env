# Shared CI coverage thresholds and ratchet policy defaults.
# Environment variables provided by CI take precedence over these defaults.

: "${BACKEND_MIN_LINE_COVERAGE:=76.1}"
: "${FLUX_MIN_LINE_COVERAGE:=52.2}"
: "${APP_MIN_LINE_COVERAGE:=6.0}"
: "${APP_COVERAGE_TARGET:=MovieSwift.app}"

# Ratchet policy: when measured coverage is at least this much above the
# configured threshold, suggest (or enforce) raising the threshold.
: "${COVERAGE_RATCHET_STEP:=0.5}"
: "${COVERAGE_RATCHET_ENFORCE:=0}"
